skip_list:
  - package-latest

# Data file (the supported-platforms list), not a playbook — ansible-lint would
# otherwise try to parse it as plays. Consumed by molecule/shared/{create,destroy}.yml
# and scripts/sync-meta-platforms.py.
exclude_paths:
  - molecule/shared/platforms.yml
