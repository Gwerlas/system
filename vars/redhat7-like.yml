---
packages:
  bridge:
    - bridge-utils
  ca_certificates:
    - ca-certificates
  chrony:
    - chrony
  common:
    - rsync
  desktop:
    - usbutils
  ntp:
    - ntp

default_time_backend: chrony
time_backends:
  chrony:
    config: chrony.conf
    service: chronyd
  ntp:
    config: ntp.conf
    service: ntpd
