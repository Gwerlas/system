---
system_packages:
  bond: ifenslave
  bridge: bridge-utils
  ca_certificates: ca-certificates
  chrony: chrony
  common:
    - rsync
  desktop:
    - usbutils
  ntp: ntp
  vlan: vlan

default_time_backend: chrony
