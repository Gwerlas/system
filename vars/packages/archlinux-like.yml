---
system_packages_update_policy:
  path: /var/lib/pacman/sync

system_packages_upgrade_policy:
  upgrade: true

system_packages:
  common:
    - ca-certificates
    - rsync
    - zsh
  desktop:
    - usbutils
