---
system_packages_update_cache_policy:
  path: /var/lib/pacman/sync

system_packages_update_policy:
  upgrade: true

system_packages:
  common:
    - ca-certificates
    - rsync
  desktop:
    - usbutils
