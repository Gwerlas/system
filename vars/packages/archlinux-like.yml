---
system_package_update_cache_policy:
  path: /var/lib/pacman/sync

system_package_update_policy:
  upgrade: true

system_packages:
  common:
    - ca-certificates
    - rsync
  desktop:
    - usbutils
