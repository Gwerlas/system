---
system_pkg_update_cache_policy:
  path: "/var/cache/{{ ansible_pkg_mgr }}"

system_pkg_update_policy:
  name: '*'
  state: latest

system_pkgs:
  common:
    - ca-certificates
    - rsync
  desktop:
    - usbutils
