---
system_package_update_cache_policy:
  path: "/var/cache/{{ ansible_pkg_mgr }}"

system_package_update_policy:
  name: '*'
  state: latest

system_packages:
  common:
    - ca-certificates
    - rsync
  desktop:
    - usbutils
