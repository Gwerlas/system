---
system_packages_update_policy:
  upgrade: dist
  autoremove: true

system_packages_update_cache_policy:
  marker: /var/cache/gwsys

system_packages:
  common:
    - ca-certificates
    - rsync
  desktop:
    - usbutils
