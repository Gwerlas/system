---
system_update_cache_policy:
  path: /var/cache/apt/archives
  pattern: lock
  age: -1w

system_update_policy:
  upgrade: dist
  autoremove: true

system_packages:
  common:
    - ca-certificates
    - rsync
  desktop:
    - usbutils
