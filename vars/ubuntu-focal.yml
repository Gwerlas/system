---
# Prevent error: Essential packages were removed and -y was used without --allow-remove-essential
masks:
  packages:
    - systemd-timesyncd

# Gnome Console is not yet available
release_packages:
  gnome-console:
    - gnome-terminal
