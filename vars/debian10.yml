---
# Disable systemd-timesyncd due to broken dependencies
default_time_backend_name: chrony

masks:
  time_backends:
    - systemd-timesyncd

# Gnome Console is not yet available
release_packages:
  gnome-console:
    - gnome-terminal
