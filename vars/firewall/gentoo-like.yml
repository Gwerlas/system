---
system_firewall_packages:
  - net-firewall/firewalld

system_firewall_config_file:
  src: package.use/firewalld
  dest: /etc/portage/package.use/firewalld
  owner: "{{ ansible_user_id }}"
  group: portage
  mode: 0664
